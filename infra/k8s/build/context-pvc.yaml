apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kaniko-context
  namespace: thunder-dev
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
